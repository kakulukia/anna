select(name="dog-names" id="dog-names")
  for chapter in chapter_list
    option(value=chapter.id)= chapter

a.btn.btn-info(style="margin-left: 10px" href="{% url 'copy_media' media_id %}") Lektion kopieren
